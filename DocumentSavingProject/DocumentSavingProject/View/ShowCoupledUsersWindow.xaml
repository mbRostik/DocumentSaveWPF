<Window x:Class="DocumentSavingProject.View.ShowCoupledUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Show Coupled Users" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Left Column: Files -->
        <Border BorderBrush="#007ACC" BorderThickness="1" CornerRadius="2" Margin="10">
            <ListBox ItemsSource="{Binding FewUsersCollectionKeys}" 
                     DisplayMemberPath="FileName"
                     SelectedItem="{Binding SelectedFile, Mode=TwoWay}" 
                     Margin="10" />
        </Border>

        <!-- Right Column: Users -->
        <Border Grid.Column="1" BorderBrush="#007ACC" BorderThickness="1" CornerRadius="2" Margin="10">
            <ListBox ItemsSource="{Binding SelectedFileUsers}" 
         SelectedItem="{Binding SelectedFileUser, Mode=TwoWay}" 
         Margin="10">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{x:Null}">
                        <!-- Default Background -->
                        <Setter Property="Background" Value="White" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="Margin" Value="5" />
                        <Setter Property="Padding" Value="10" />
                        <!-- Hover Effect -->
                        <Setter Property="Control.Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="LightGreen" />
                                <Setter Property="Foreground" Value="Black" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="10,0,0,0" />
                            <TextBlock Text="{Binding SurName}" Margin="10,0,0,0" />
                            <TextBlock Text="{Binding PROGRESSIVO}" Margin="10,0,0,0" />
                        </StackPanel>


                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
